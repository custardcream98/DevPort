import styled from "@emotion/styled";

const Line = () => {
  return <StyledLine />;
};

const StyledLine = styled.div`
  width: 100%;
  height: 4px;
  background-color: #f0f0f0;

  margin: 1rem 0 3rem;
`;

export default Line;
